name: PS4 Nightly Build

on:
  push:
    paths:
      - make/Makefile_ps4
      - source/scratch/**
      - source/sdl/**
      - gfx/menu/**
      - include/**
      - .github/workflows/nightly-ps4.yml
      - docker/Dockerfile.ps4
      - source/main.cpp
    branches:
      - main
  workflow_dispatch:

jobs:
  nightly:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: Create `romfs` directory
        run: mkdir romfs
      - name: Build and Export
        uses: docker/build-push-action@v5
        with:
          context: .
          file: docker/Dockerfile.ps4
          target: exporter
          outputs: type=local,dest=.
      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Scratch Everywhere! PS4 Nightly
          path: scratch-ps4.pkg
