name: GameCube Nightly Build

on:
  push:
    paths:
      - make/Makefile_gamecube
      - source/scratch/**
      - source/sdl/**
      - include/**
      - gfx/gamecube/**
      - gfx/menu/**
      - .github/workflows/nightly-gamecube.yml
      - docker/Dockerfile.gamecube
      - source/main.cpp
    branches:
      - main
  workflow_dispatch:

jobs:
  nightly:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: Create `romfs` directory
        run: mkdir romfs
      - name: Build and Export
        uses: docker/build-push-action@v5
        with:
          context: .
          file: docker/Dockerfile.gamecube
          target: exporter
          outputs: type=local,dest=.
      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Scratch Everywhere! GameCube Nightly
          path: scratch-gamecube.dol
